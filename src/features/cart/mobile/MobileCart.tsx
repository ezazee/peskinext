"use client";

import type { CartData } from "@data/index";

export function CartMobile({ initial }: { initial: CartData }) {
  return (
    <div className="p-4">
      {/* TODO: implementasi mobile nanti */}
      <p className="text-sm text-gray-500">Cart mobile (coming soon)</p>
    </div>
  );
}
